

export const metadata = {
  title: 'Cliente',
  description: 'PROJETO INTERDICIPLINAR',
}

export default function RootLayout({ children }) {
  return (
    <html lang="pt-br">
      <body>{children}</body>
    </html>
  )
}
